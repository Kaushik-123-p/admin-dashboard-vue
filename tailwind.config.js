/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html", // for plain HTML files
    "./src/**/*.{js,ts,jsx,tsx,vue}", // for all JS, TS, JSX, TSX, and Vue files in the 'src' folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
